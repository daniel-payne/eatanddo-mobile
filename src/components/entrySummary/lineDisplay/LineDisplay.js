import React from "react";
import { observer } from "mobx-react";

import Button from "@material-ui/core/Button";

const LineDisplay = props => {
  return <Button>LineDisplay</Button>;
};

export default observer(LineDisplay);
